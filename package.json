{
  "name": "phonebook-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "lint": "tslint -p . -c tslint.json",
    "start": "npm run build && node ./build/app.js",
    "start:watch": "npm run build:watch & nodemon ./build/app.js",
    "build": "rm -rf ./build && tsc",
    "build:watch": "npm run build -- -w",
    "docker:start": "docker-compose run --service-ports --rm -v $(pwd):/app api npm run start",
    "docker:start:watch": "docker-compose run --rm -v $(pwd):/app api npm run start:watch",
    "docker:test": "docker-compose run -u root --rm -v $(pwd):/app api npm run test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/c-kle/phonebook-api.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/c-kle/phonebook-api/issues"
  },
  "homepage": "https://github.com/c-kle/phonebook-api#readme",
  "dependencies": {
    "@types/node": "^10.12.7",
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "jsonwebtoken": "8.4.0",
    "multer": "^1.4.1",
    "mysql2": "^1.6.4",
    "ramda": "^0.25.0",
    "reflect-metadata": "^0.1.12",
    "routing-controllers": "^0.7.7",
    "typedi": "0.7.2",
    "typeorm": "0.2.9",
    "typeorm-typedi-extensions": "0.2.1"
  },
  "devDependencies": {
    "@types/body-parser": "^1.17.0",
    "@types/express": "^4.16.0",
    "@types/jsonwebtoken": "8.3.0",
    "@types/multer": "^1.3.7",
    "@types/ramda": "github:types/npm-ramda#dist",
    "nodemon": "1.14.11",
    "tslint": "^5.12.1",
    "typescript": "^3.1.6"
  }
}
